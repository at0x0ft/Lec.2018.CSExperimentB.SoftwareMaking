package game.cards;

public class Minister extends Card {
public Minister(int id) {
        super("Minister", 7, "If sum of your cards' strength (your hand and draw cards) is 12 or more, you'll lose right then.", id);
    }
}